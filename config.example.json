{
    "timezone": "",                     // Timezone format override. Uses your system clock if undefined
    "nicknames": {
        "username_0": "name_0",         // Snapchat exports do not include YOUR friend usernames
        "username_1": "name_1",         // This mapping overrides usernames if you'd prefer it to
        "username_2": "name_2"
    },
    "colors": {
        "sent": "#0faeff",              // Sent message color
        "received": "#d93851",          // Recieved message color
        "background": "#1e1e1e"
    }
}